{
  "name": "quark-crm",
  "version": "0.0.4",
  "description": "Quark CRM",
  "main": "dist/main.js",
  "scripts": {
    "nodemon": "nodemon",
    "app": "electron .",
    "setup": "npm install && npm run svelte:install",
    "start": "export $(cat .env | xargs) && npm run compile && electron --trace-warnings . --no-sandbox",
    "svelte:install": "cd svelte && npm install",
    "svelte:dev": "cd svelte && npm run dev",
    "svelte:build": "cd svelte && npm run build",
    "compile": "tsc && node scripts/env-replace.js && copyfiles -u 1 electron/*.png dist",
    "build": "npm run svelte:build && npm run compile && electron-builder build --win --linux --publish never",
    "publish": "npm run svelte:build && npm run compile && electron-builder build --win --linux --publish always",
    "out:win": "npm run svelte:build && npm run compile && electron-builder build --win --publish never",
    "publish:win": "npm run svelte:build && npm run compile && electron-builder build --win --publish always",
    "out:linux": "npm run svelte:build && npm run compile && electron-builder build --linux --publish never",
    "publish:linux": "npm run svelte:build && npm run compile && electron-builder build --linux --publish always",
    "bump": "npm version patch"
  },
  "author": "Travja",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.4.5",
    "electron": "^31.3.1",
    "electron-builder": "^24.13.3",
    "nodemon": "^3.1.4",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "build": {
    "appId": "dev.travja.quark",
    "publish": {
      "provider": "github",
      "owner": "Travja",
      "repo": "quark-crm"
    },
    "directories": {
      "output": "out"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "electron/quark-logo.png",
      "publisherName": "Travja"
    },
    "linux": {
      "maintainer": "Travja <the.only.t.craft@gmail.com>",
      "icon": "electron/quark-logo.png",
      "target": [
        "deb"
      ]
    },
    "nsis": {
      "installerIcon": "electron/icon.ico",
      "uninstallerIcon": "electron/icon.ico",
      "uninstallDisplayName": "Quark",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "electron-fetch": "^1.9.1",
    "electron-serve": "^2.1.0",
    "electron-updater": "^6.3.4"
  }
}
