{
  "name": "quark-crm",
  "version": "1.0.0",
  "description": "Quark CRM",
  "main": "dist/main.js",
  "scripts": {
    "nodemon": "nodemon",
    "app": "electron .",
    "start": "export $(cat .env | xargs) && npm run compile && electron --trace-warnings . --no-sandbox",
    "svelte:dev": "cd svelte && npm run dev",
    "svelte:build": "cd svelte && npm run build",
    "compile": "tsc && node scripts/env-replace.js && copyfiles -u 1 electron/*.png dist",
    "out:win": "export $(cat .env | xargs) && export NODE_ENV=production && npm run svelte:build && npm run compile && export $(cat .env | xargs) && electron-builder build --win --publish never",
    "publish:win": "export $(cat .env | xargs) && export NODE_ENV=production && npm run svelte:build && npm run compile && export $(cat .env | xargs) && electron-builder build --win --publish always",
    "out:linux": "export $(cat .env | xargs) && export NODE_ENV=production && npm run svelte:build && npm run compile && export $(cat .env | xargs) && electron-builder build --linux --publish never",
    "publish:linux": "export $(cat .env | xargs) && export NODE_ENV=production && npm run svelte:build && npm run compile && export $(cat .env | xargs) && electron-builder build --linux --publish always"
  },
  "author": "Travja",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.4.5",
    "electron": "^31.3.1",
    "electron-builder": "^24.13.3",
    "electron-updater": "^6.2.1",
    "nodemon": "^3.1.4",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "build": {
    "appId": "dev.travja.quark",
    "directories": {
      "output": "out"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "icon.ico"
    },
    "linux": {
      "maintainer": "Travja <the.only.t.craft@gmail.com>",
      "icon": "electron/quark-logo.png",
      "target": [
        "deb"
      ]
    },
    "nsis": {
      "installerIcon": "icon.ico",
      "uninstallerIcon": "icon.ico",
      "uninstallDisplayName": "Quark",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "electron-fetch": "^1.9.1",
    "electron-serve": "^2.1.0"
  }
}
